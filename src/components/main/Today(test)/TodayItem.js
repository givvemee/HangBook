import React from 'react';

const TodayItem = ({recommend}) => {
    const {title} = recommend
    return (
        <div>
            <h2>ㅠㅠㅠ</h2>
           <p>{title}</p>
        </div>
    );
};

export default TodayItem;